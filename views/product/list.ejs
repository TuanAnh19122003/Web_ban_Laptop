<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Danh Sách Sản Phẩm</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .product-img {
            width: 100px;
            height: auto;
        }
        .container {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 class="my-4">Danh Sách Sản Phẩm</h1>
        <a href="/products/create" class="btn btn-primary mb-3">Thêm Sản Phẩm</a>
        <div class="list-group">
            <% if (products && products.length) { %>
                <% products.forEach(function(product) { %>
                <div class="list-group-item d-flex justify-content-between align-items-center">
                    <div class="d-flex align-items-center">
                        <img src="/<%= product.imageUrl %>" alt="<%= product.name %>" class="product-img mr-3">
                        <div>
                            <h5 class="mb-1"><%= product.name %></h5>
                            <p class="mb-1"><strong>Giá:</strong> <%= product.price %> VNĐ</p>
                            <p class="mb-1"><strong>Danh Mục:</strong> <%= product.category %></p>
                            <p class="mb-1"><strong>Thương Hiệu:</strong> <%= product.brand %></p>
                        </div>
                    </div>
                    <div>
                        <a href="/products/edit/<%= product._id %>" class="btn btn-warning btn-sm">Sửa</a>
                        <a href="/products/delete/<%= product._id %>" class="btn btn-danger btn-sm">Xóa</a>
                        <a href="/products/details/<%= product._id %>" class="btn btn-info btn-sm">Chi Tiết</a>
                    </div>
                </div>
                <% }); %>
            <% } else { %>
                <p>Không có sản phẩm nào.</p>
            <% } %>
        </div>
    </div>

    <!-- Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.3/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
